---
export interface Props {
	padding?: boolean;
}

const { padding = true } = Astro.props;
---

<div class="border-b border-stone-800">
	<div class="container mx-auto">
		<div class={`border-x border-stone-800 mx-12 ${padding ? 'p-12' : ''}`}>
			<slot />
		</div>
	</div>
</div>
